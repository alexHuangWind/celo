<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:id="@+id/rl_item_recommend"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/list_selector">

    <ImageView
        android:id="@+id/iv_picture_image"
        android:layout_width="140dp"
        android:layout_height="90dp"
        android:padding="10dp"
        android:scaleType="centerCrop"/>

    <TextView
        android:id="@+id/tv_title_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/iv_picture_image"
        android:layout_marginRight="10dp"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@id/iv_picture_image"
        android:text="Text Text Text Text Text Text"
        android:textColor="#222831"
        android:textSize="15sp"/>

    <TextView
        android:id="@+id/tv_gender_DOB"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/iv_picture_image"
        android:layout_marginBottom="4dp"
        android:layout_toRightOf="@id/iv_picture_image"
        android:text="2016-09-01"
        android:textColor="@android:color/black"
        android:textSize="12sp"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_below="@+id/iv_picture_image"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="10dp"
        android:background="@color/colorTxtUnSelected"/>
</RelativeLayout>
